# -*- coding: utf-8 -*-
"""pix2pix.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1bK4X4SsO242wX6kOb8sEO0QIxAYabWFx
"""
#Getting the dataset
import cv2 as cv
import torch
import numpy
from google.colab.patches import cv2_imshow
import matplotlib.pyplot as plt
from google.colab import files
files.upload()
#!rm -r ~/.kaggle
!mkdir ~/.kaggle
!mv ./kaggle.json ~/.kaggle/ #When prompted, upload kaggle.json file (pre-downloaded)
!chmod 600 ~/.kaggle/kaggle.json
!kaggle datasets download -d badasstechie/celebahq-resized-256x256
! unzip celebahq-resized-256x256.zip -d imgs

!git clone https://github.com/junyanz/pytorch-CycleGAN-and-pix2pix

import os
os.chdir('pytorch-CycleGAN-and-pix2pix/')

!pip install -r requirements.txt

!bash ./scripts/download_pix2pix_model.sh facades_label2photo

# Commented out IPython magic to ensure Python compatibility.     //This file takes each image from the folder, makes the sketch and stores the sketch in a new folder (as separate train and test folders) before sending it to the model.
# %%writefile /content/sketch.py                                  //Uploaded as a separate file in the same folder
# import cv2 as cv
# import numpy
# from google.colab.patches import cv2_imshow
# import matplotlib.pyplot as plt
# import os
# from pathlib import Path
# p = Path('pics')
# os.mkdir(p)
# os.chdir(p)
# os.mkdir('train')
# os.mkdir('test')
# os.chdir('/content/pytorch-CycleGAN-and-pix2pix')
# 
# for i in range(30000):
#   images_path = "/content/imgs/celeba_hq_256"
#   if i<10:
#     each=Path('0000'+str(i)+'.jpg')
#   elif i<100:
#     each=Path('000'+str(i)+'.jpg')
#   elif i<1000:
#     each=Path('00'+str(i)+'.jpg')
#   elif i<10000:
#     each=Path('0'+str(i)+'.jpg')
#   new = os.path.join(images_path,each)
# 
#   if i<1000:
#    new2 = os.path.join("/content/pytorch-CycleGAN-and-pix2pix/pics/train",each)
#   else:
#    new2 = os.path.join("/content/pytorch-CycleGAN-and-pix2pix/pics/test",each)
#   image = cv.imread(new,1)
#   im1 = cv.cvtColor(cv.cvtColor(image,cv.COLOR_BGR2RGB),cv.COLOR_RGB2GRAY)
# 
#   im2 = cv.GaussianBlur(cv.bitwise_not(im1),(21, 21),sigmaX=0, sigmaY=0)
#   im3 = cv.divide(im1,255-im2,scale=255)
#   im_3_2 = cv.cvtColor(im3,cv.COLOR_GRAY2BGR)
#   im4 = cv.hconcat([image[...,::-1],im_3_2])
#   plt.axis('off')
#   plt.imshow(im4,cmap="gray")
#   plt.savefig(new2)

!python /content/sketch.py #This step takes hours... till now, due to Colab usage time limits, I could not complete the pre-processing of all 30k images here

!python train.py --dataroot /content/pytorch-CycleGAN-and-pix2pix/pics  --name facades_pix2pix --model pix2pix --direction BtoA --display_id -1 #Due to the above, this was executed only when the previous one was interrupted
